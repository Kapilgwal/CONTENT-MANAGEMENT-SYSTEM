<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Article</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="min-h-screen bg-gray-900 text-white flex flex-col items-center py-12">
        <div class="w-full max-w-3xl mx-auto p-6 bg-gray-800 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold mb-6 text-center text-yellow-500">Update Post</h1>
            <a href="/home">Home</a>
            
            <form class="space-y-6" action="/update/<%= post._id %>" method="post">
                <!-- Title Field -->
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-400">Title</label>
                    <textarea id="title" name="title" 
                        class="block w-full p-4 resize-none bg-transparent border-2 border-grey-700 rounded-md outline-none focus:border-yellow-500 transition duration-300 ease-in-out text-gray-400">
                        <%= post.title %>
                    </textarea>
                </div>
                
                <!-- Content Field -->
                <div>
                    <label for="content" class="block text-sm font-medium text-gray-400">Content</label>
                    <textarea id="content" name="content"
                        class="block w-full p-4 resize-none h-64 bg-transparent border-2 border-gray-700 rounded-md outline-none focus:border-yellow-500 transition duration-300 ease-in-out text-gray-400">
                        <%= post.content %>
                    </textarea>
                </div>
                <div class="text-center">
                        <input type="submit" value="Update Post" class="px-6 py-3 bg-yellow-500 text-gray-900 font-semibold rounded-md shadow hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:ring-opacity-50 transition duration-300 ease-in-out">
                </div>
            </form>
        </div>
    </div>
    


</body>

</html>